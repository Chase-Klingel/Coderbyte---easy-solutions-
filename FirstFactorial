function FirstFactorial(num) { 
   // create a for loop that is initialized at num - 1
   // stop the loop when i = 2 and decrease i on each pass
   for (var i = num - 1; i > 1; i--) {
      // multiply num * i 
      num *= i;
   }
   // return the final num
   return num;
}
